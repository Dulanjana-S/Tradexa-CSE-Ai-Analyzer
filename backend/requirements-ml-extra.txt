# Optional advanced ML/NLP stack.
# Install this only on machines that support these wheels.
# The app works without them and falls back to sklearn + event rules.
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2; python_version < '3.13'
transformers>=4.41
torch>=2.2; python_version < '3.13'
